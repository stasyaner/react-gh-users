import React from "react";
import { Container } from "react-bootstrap";

const Home: React.FC = () => (
    <Container className="my-5">
        <h3>home</h3>
    </Container>
);

export default Home;
